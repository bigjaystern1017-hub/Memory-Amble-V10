I want to do PHASE 2 of the MemoryAmble UI/UX redesign.

Important: Phase 1 was visual polish. Phase 2 should add a lightweight visual “Today’s Route” / “Memory Walk” panel that makes the memory palace feel tangible.

Do NOT rebuild the app. Do NOT change the beat engine. Do NOT change the API calls, Supabase auth, localStorage keys, lesson progression, object assignment logic, recall scoring, or session saving.

The goal is to visualize existing state, not create a new flow.

Core UX idea:
MemoryAmble should feel like a guided memory walk. As the user chooses stops, places objects, and recalls items, the interface should show a simple route panel so the user can see their palace taking shape.

Relevant existing files:
- client/src/pages/amble.tsx
- client/src/components/beat-engine.ts
- client/src/components/chat-message.tsx
- client/src/components/chat-input.tsx
- client/src/components/progress-bar.tsx
- client/src/index.css

NEW COMPONENT:
Create a new component:

client/src/components/memory-route-panel.tsx

This component should receive existing state from amble.tsx. It should not own the core lesson state.

Suggested props:

type MemoryRoutePanelProps = {
  placeName?: string;
  stops?: string[];
  assignments?: Array<{
    stopName?: string;
    object?: string;
  }>;
  userScenes?: string[];
  currentStepIndex?: number;
  currentBeat?: string;
};

Visual design:
- A polished white/cream card.
- Rounded-2xl corners.
- Soft border.
- Subtle shadow.
- Header:
  “Today’s Route”
  Subtext: “Your memory palace is taking shape.”
- If placeName exists, show it as:
  “Place: [placeName]”
- Show a vertical route list.
- Each row should include:
  - numbered circle: 1, 2, 3, etc.
  - stop name if available
  - object name if assigned
  - optional scene preview if userScenes has content
- Current active stop should have purple accent.
- Completed/filled stops should have pale lavender or check-style treatment.
- Empty future stops should appear muted.
- Keep the panel calm and readable.

Display examples:

If only stops exist:
1. Front Door
2. Hallway
3. Kitchen

If assignments exist:
1. Front Door — Pineapple
2. Hallway — Umbrella
3. Kitchen — Blue Sneaker

If userScenes exist:
1. Front Door — Pineapple
   “A pineapple the size of a refrigerator blocks the door.”

Empty state:
If no stops are available yet, show:
“Your route will appear here as you build it with Timbuk.”

Integration in amble.tsx:
- Import MemoryRoutePanel.
- Pass it existing state:
  - state.placeName
  - state.stops
  - state.assignments
  - state.userScenes
  - state.stepIndex
  - currentBeat
- Do not mutate state from the panel.
- Do not add new save behavior.
- Do not change the lesson flow.

Layout:
On desktop:
- In the chat phase, use a two-column layout:
  - left/main column: Timbuk guide card/messages
  - right/sidebar column: Today’s Route panel
- Main column should remain dominant.
- Sidebar should be around 300–360px wide.
- Overall content max-width around 1180–1240px.
- The bottom response tray should still span comfortably beneath or align with the main layout.
- On smaller screens, stack the route panel below the Timbuk card or hide/collapse it cleanly.

Important:
- The panel should only appear during the chat/training phase, not on every onboarding screen unless that already makes sense.
- Do not create fake data.
- Do not invent a palace illustration.
- Do not replace the chat flow.
- Do not create new steps.
- Do not change getProgressStep.
- Do not change beat IDs.
- Do not break the typewriter.
- Do not break recall.
- Do not break session saving.

Optional small improvement:
If currentBeat suggests the user is in object placement or recall, make the panel title slightly contextual:
- During place-building: “Build Your Route”
- During object placement: “Planting Images”
- During recall: “Walk It Back”
But this should be visual text only. Do not change logic.

Implementation priority:
1. Create MemoryRoutePanel component.
2. Add it beside the existing chat area in amble.tsx.
3. Make it responsive.
4. Keep the existing app functional.

After implementation, summarize:
1. Files changed.
2. Where MemoryRoutePanel is used.
3. What existing state it reads.
4. What logic was intentionally left untouched.