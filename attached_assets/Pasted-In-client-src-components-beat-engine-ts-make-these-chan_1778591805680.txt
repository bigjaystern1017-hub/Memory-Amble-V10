In client/src/components/beat-engine.ts, make these changes:
1. Make these beats require a continue button instead of auto-advancing:
In the beatNeedsContinueButton function, add these beats so they return true:

react-stop — after Timbuk confirms a stop name, the user should see his reaction and tap to continue. Currently it auto-chains to the next stop or to assigning.
practice-intro — after Timbuk says "Before we place your items — a quick practice," the user should tap to continue. Currently it auto-chains to practice-item.
practice-done — after the pineapple practice is confirmed, the user should tap before moving to real placement.
placement-intro — after Timbuk introduces the real placement round, the user should tap to continue.
walkthrough-intro — after Timbuk introduces the recall walk, the user should tap to continue.

2. Add continue button labels for these beats:
In the getContinueButtonLabel function, add cases:

react-stop: if there are more stops to name, return "Next stop →". If all stops are named, return "Let's go →"
practice-intro: return "I'm ready →"
practice-done: return "Ready for the real thing →"
placement-intro: return "Let's place them →"
walkthrough-intro: return "Let's walk →"

Now in client/src/pages/amble.tsx, make this change:
3. Remove the typing indicator ("...") from the fixed stage.
Find where the isTyping state causes a typing indicator bubble (the "..." dots) to render in the stage area. Remove it entirely — in the fixed stage model, we don't need a typing simulation. When Timbuk's next message is loading (API call or processing), just show nothing until the message is ready. The stage holds the previous content until the new content replaces it via crossfade.
Specifically, find any JSX that renders a ChatMessage with isTyping prop and remove or hide those elements. The isTyping state can stay in the code (it may be used for other logic), but it should NOT render any visible "..." indicator in the stage.
4. Remove the scrollToBottom calls.
In the fixed stage model, nothing scrolls. Find all calls to scrollToBottom() inside advanceBeat, showTimbukWithTypewriter, addUserMessage, and showWisdomMessage. Remove them or comment them out. The stage content replaces in place — there is nothing to scroll to.
Do not change the landing page, education slides, start page, or any server files.
Do not change anything else.