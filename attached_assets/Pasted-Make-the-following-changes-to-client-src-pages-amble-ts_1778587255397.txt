Make the following changes to client/src/pages/amble.tsx. Read ALL instructions before starting.
1. CONVERT TIMBUK MESSAGES TO A FIXED "STAGE" AREA
The current behavior: Timbuk messages stack in a scrolling chat feed, pushing old messages up. New behavior: Only the LATEST Timbuk message is shown at full size in a fixed "stage" area at the top of the main content. Previous messages should fade out or be hidden entirely — they should NOT stack and scroll.
Implementation: Instead of rendering all messages in a scrolling list, keep only the most recent Timbuk message visible in the main content area. You can keep a "Show earlier messages" or "Transcript" toggle link below it that expands to show the conversation history, but it should be collapsed by default. The active Timbuk message card should have a fixed/stable height container — when a new message replaces the old one, the container should NOT change size in a way that pushes other content around. Pre-reserve the space, then fade the new text in.
2. OBJECT CARD SEQUENCING
The object card (MemoryObjectCard / "Plant this image" card) currently appears at the same time as Timbuk's message, creating visual competition. Change the timing:

When a new placement beat starts: Timbuk's message appears FIRST in the stage area
After a 400ms delay, the object card fades/slides in below Timbuk's message
The object card should be positioned directly ABOVE the input tray, not floating in the middle of the chat area. Vertical sequence: Timbuk message (top) → Object card (middle) → Input tray (bottom)
When the user submits their response: the object card transforms into a "planted" confirmation state (show the object name, location, and the user's description with a checkmark). Hold this for 2 seconds.
Then the confirmation card collapses/fades and the item appears in the route panel on the right
Timbuk's reaction message replaces his previous message in the stage area
A "Continue" or "Next →" button appears. Nothing auto-advances — the user controls the pace.
On tap of Continue: Timbuk's next instruction replaces the current one, and after 400ms the next object card appears

3. RECALL CARD COLOR
The "Walk it back" recall card is currently using red/coral colors. Change it to a warm amber/gold scheme:

Card border: #D4A843 (warm gold)
Card header/title: #8B6914 (dark gold)
Card background: #FFF8E7 (very light warm yellow)
Keep green for correct answers and a soft gray for incorrect (not red — red feels punishing)

4. AUTO-FOCUS THE INPUT
After every Timbuk message that requires user input (placement descriptions, recall answers, stop naming), automatically focus the input field so the user's cursor is already in the text box and they can start typing immediately without clicking into it.
5. SOUND-ON BANNER FADE
The "Sound on — Timbuk is more fun with sound" banner should fade out automatically after 4 seconds. Add a CSS transition: after 4 seconds, set opacity to 0 over 1 second, then set display to none.
6. FIX "WELCOME BACK" FOR NEW USERS
In the onboard-welcome beat message, check if this is actually a returning user (currentDay > 1 or has saved palace data) before saying "Welcome back." For first-time users, say "Welcome" not "Welcome back."
7. NOTHING CLEARS UNTIL USER IS READY
Remove any auto-advance timers that clear content before the user has acted. Specifically:

After Timbuk reacts to a placement, do NOT auto-advance to the next object. Show a "Next →" button.
After the Amble Scroll / results screen appears, do NOT auto-clear it. The user should be able to read it for as long as they want.
After Timbuk's post-recall reaction, do NOT auto-advance. Show "Continue →".
The only auto-timed element should be the sound-on banner fading (item 5 above).

Do not change beat-engine.ts, landing.tsx, or any server files. Only change amble.tsx and any components it imports (memory-object-card.tsx, recall-walk-panel.tsx, etc.) as needed to implement these layout changes.
Do not change anything else.