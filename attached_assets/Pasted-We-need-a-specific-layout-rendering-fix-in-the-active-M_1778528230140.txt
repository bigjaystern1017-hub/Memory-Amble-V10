We need a specific layout/rendering fix in the active MemoryAmble lesson.

Problem:
The current chat UI renders the full messages array as a vertical feed. New Timbuk messages append to the list and push previous bubbles upward. This causes content to get cut off behind the sticky header or bottom composer.

Goal:
Keep the existing message state and lesson logic exactly as-is, but change the active lesson render so the main stage shows only the latest relevant Timbuk message instead of the full scrolling transcript.

Important:
Do NOT change the beat engine.
Do NOT change getNextBeat.
Do NOT change getTimbukMessage.
Do NOT change addUserMessage.
Do NOT change showTimbukWithTypewriter.
Do NOT change showWisdomMessage.
Do NOT change how messages are stored in state.
Do NOT delete or truncate the messages array.
Do NOT change API calls, auth, localStorage, recall scoring, or session saving.

This is a render/layout change only.

Files to inspect:
- client/src/pages/amble.tsx
- client/src/components/chat-message.tsx
- client/src/components/chat-input.tsx
- client/src/components/memory-object-card.tsx
- client/src/components/memory-route-panel.tsx

Implementation:

1. In client/src/pages/amble.tsx, derive these values before render:

const latestTimbukMessage = [...messages].reverse().find(m => m.sender === "timbuk");
const latestUserMessage = [...messages].reverse().find(m => m.sender === "gladys");

Also derive:
const hasMessages = messages.length > 0;

2. In the active chat/lesson phase render, replace the full messages.map(...) feed in the main visible lesson area with a stable “current lesson stage.”

The main stage should render:

- If isTyping is true and there is no latestTimbukMessage currently revealing:
  show a single Timbuk typing/loading card.

- If latestTimbukMessage exists:
  render one ChatMessage using latestTimbukMessage only.

- If latestUserMessage exists and it is more recent than the latest Timbuk message OR useful to show:
  render it as a compact user response chip above or below the Timbuk card, not a full giant bubble.

Do not render every message in the main stage.

3. Keep the full transcript accessible but secondary.

Add a small collapsed details section or button below the main stage:

<button>Review earlier messages</button>

When clicked, show the full transcript in a small scrollable drawer/panel using messages.map(...).

This should be collapsed by default.

If implementing the drawer is too much, skip it for now. The required part is: main stage should only show the latest Timbuk message.

4. Stable main stage layout.

Wrap the latest Timbuk message in a container like:

<div className="lesson-stage">
  ...
</div>

Use Tailwind classes roughly equivalent to:

className="relative w-full min-h-[280px] md:min-h-[340px] flex flex-col justify-center"

The point:
- Reserve space for the active card.
- Do not let the page jump every time a new message appears.
- The current card should appear inside a stable area.

5. Prevent header/composer overlap.

The main lesson scroll/content container must include:
- padding-top enough to clear the sticky progress/header area if needed
- padding-bottom enough to clear the bottom composer

Use something like:
className="pb-40 md:pb-44"

If the composer is sticky/fixed, the content area must have bottom padding equal to or greater than the composer height.

6. Object card placement.

The MemoryObjectCard should appear below the latest Timbuk message only when it is relevant.

Use explicit logic:

const isObjectPlacementBeat =
  currentBeat === "place-object" ||
  currentBeat === "mirror-object" ||
  currentBeat === "react-practice" ||
  currentBeat.includes("object");

Only show MemoryObjectCard when:
- state.assignments exists
- state.assignments.length > 0
- isObjectPlacementBeat is true

Do not show MemoryObjectCard during general setup messages unless there is a current assignment.

7. Route panel stays on the right.

Keep MemoryRoutePanel as the right sidebar on desktop.

Do not let the right sidebar force the main card to become too narrow.

Suggested layout:
<div className="grid grid-cols-1 xl:grid-cols-[minmax(0,1fr)_360px] gap-8 max-w-7xl mx-auto">

8. Compact the active lesson stepper.

Do not redesign the stepper globally.

In active chat/lesson phase only, reduce vertical padding around the ProgressBar wrapper.

Example:
- onboarding wrapper can keep py-6 or py-8
- active lesson wrapper should use py-3

Do not change getProgressStep.

9. ChatMessage animation safety.

Do not change the animation yet except to ensure it does not affect layout height.

The latest message card should render inside the reserved lesson-stage area.

10. Preserve callbacks.

When rendering latestTimbukMessage, preserve all existing props currently passed to ChatMessage:
- sender
- text
- typewriter
- variant
- onTypewriterDone
- fastForward
- onSkipTyping
- isLatest

Do not break typewriter/reveal completion.

Expected result:
- The main lesson area shows one current Timbuk instruction card at a time.
- The screen no longer feels like a scrolling chat feed.
- Previous messages do not push the current card behind the header.
- MemoryObjectCard does not get covered by the bottom composer.
- The route panel remains visible and useful.

After implementation, summarize:
1. Where the full messages.map render was changed.
2. How latestTimbukMessage is derived.
3. Whether a transcript drawer was added or skipped.
4. How bottom composer overlap was prevented.
5. What logic was intentionally untouched.